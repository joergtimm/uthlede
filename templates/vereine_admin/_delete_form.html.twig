<form method="post" action="{{ path('vereine_admin_delete', {'id': vereine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vereine.id) }}">
    <button class="btn">Delete</button>
</form>
