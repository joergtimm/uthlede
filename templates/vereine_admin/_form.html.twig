{{ form_start(form) }}
<div class="row">
    <div class="col-12">
        {{ form_row(form.titel) }}
    </div>
</div>
<div class="row">
    <div class="col-6">
        {{ form_row(form.logo) }}
        {{ form_row(form.beschreibung) }}
    </div>
    <div class="col-6">

    </div>
</div>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
