<form method="post" action="{{ path('app_admin_tankstellen_delete', {'id': tankstellen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tankstellen.id) }}">
    <button class="btn">Delete</button>
</form>
