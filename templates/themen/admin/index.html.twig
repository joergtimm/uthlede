{% extends 'admin_base.html.twig' %}

{% block title %}Themen index{% endblock %}

{% block content %}
<div
        class="container-fluid mt-4"
        {{ stimulus_controller('reload-content', {
            url: path('admin_themen:index', { ajax: 1 })
        }) }}
        data-action="modal-form:success->reload-content#refreshContent"
>
    <div class="d-flex flex-row">
        <h1 class="me-3">Themen index</h1>
        <div {{ stimulus_controller('modal-form', {
            formUrl: path('admin_themen:new'),
            modalTitel: 'Thema hinzufügen',
            modalOk: 'speichern',
            modalCancel: 'abbrechen'
        }) }}>
            <button class="btn btn-primary btn-sm" data-action="modal-form#openModal">+ Add</button>
            {{ include('_modal.html.twig') }}
        </div>
    </div>
    <div data-reload-content-target="content">

    {{ include('themen/admin/_list.html.twig') }}
    </div>
    <a href="{{ path('admin_themen:new') }}">Create new</a>
</div>
{% endblock %}
