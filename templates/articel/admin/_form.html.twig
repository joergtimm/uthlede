{{ form_start(form) }}
<div class="row">
    <div class="col-12">
        {{ form_row(form.titel) }}
    </div>
</div>
<div class="row">
    <div class="col-12">
        {{ form_row(form.haupttext) }}
    </div>
</div>
<div class="row">
    <div class="col-6">
        {{ form_row(form.bild) }}
    </div>
    <div class="col-6">

    </div>
</div>
<div class="row">
    <div class="col-4">
        {{ form_row(form.datum) }}
    </div>
    <div class="col-4">
        {{ form_row(form.thema) }}
    </div>
    <div class="col-4">
        {{ form_row(form.author) }}
    </div>
</div>
<div class="d-flex justify-end">
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
</div>

{{ form_end(form) }}
