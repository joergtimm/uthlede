{% extends 'base.html.twig' %}

{% block title %}ArtikelBilder{% endblock %}

{% block body %}
    <h1>ArtikelBilder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artikel_bilder.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ artikel_bilder.filename }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ artikel_bilder.createAt ? artikel_bilder.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ artikel_bilder.text }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ artikel_bilder.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artikel_bilder_index') }}">back to list</a>

    <a href="{{ path('artikel_bilder_edit', {'id': artikel_bilder.id}) }}">edit</a>

    {{ include('artikel_bilder/_delete_form.html.twig') }}
{% endblock %}
